CHIP A6Q2201837629 {
    IN a, b, c, d;  // Inputs: 4-bit binary
    OUT f;          // Output: Function f

    PARTS:
    // NOT gates
    Not(in=a, out=NotA);
    Not(in=b, out=NotB);
    Not(in=c, out=NotC);
    Not(in=d, out=NotD);

    // AND gates
    // Example: Combine three inputs (¬a ∧ ¬b ∧ ¬c)
    And(a=NotA, b=NotB, out=Partial1);  // Line 14
    And(a=Partial1, b=NotC, out=Partial2);

    // Final output
    Or(a=Partial2, b=d, out=f);
}